<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>de.spacepotato</groupId>
	<artifactId>sagittarius-parent</artifactId>
	<version>${revision}</version>
	<packaging>pom</packaging>

	<modules>
		<module>sagittarius-limbo</module>
		<module>sagittarius-network</module>
		<module>sagittarius-api</module>
		<module>sagittarius-chat</module>
		<module>sagittarius-nbt</module>
		<module>sagittarius-log</module>
		<module>sagittarius-config</module>
		<module>sagittarius-viaversion</module>
	</modules>

	<properties>
		<!-- CI-friendly version -->
		<revision>1.2.3</revision>

		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

		<!-- External dependency versions -->
		<lombok.version>1.18.30</lombok.version>
		<netty.version>4.1.111.Final</netty.version>
		<log4j.version>2.23.1</log4j.version>
		<gson.version>2.11.0</gson.version>
		<toml4j.version>0.7.2</toml4j.version>
		<hppc.version>0.9.1</hppc.version>
		<viaversion.version>5.0.0</viaversion.version>
		<snakeyaml.version>2.2</snakeyaml.version>
		<guava.version>33.2.1-jre</guava.version>

		<!-- Plugin versions -->
		<maven.compiler.plugin.version>3.11.0</maven.compiler.plugin.version>
		<maven.shade.plugin.version>3.5.1</maven.shade.plugin.version>
		<flatten.plugin.version>1.6.0</flatten.plugin.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<!-- Internal modules -->
			<dependency>
				<groupId>de.spacepotato</groupId>
				<artifactId>sagittarius-api</artifactId>
				<version>${revision}</version>
			</dependency>
			<dependency>
				<groupId>de.spacepotato</groupId>
				<artifactId>sagittarius-chat</artifactId>
				<version>${revision}</version>
			</dependency>
			<dependency>
				<groupId>de.spacepotato</groupId>
				<artifactId>sagittarius-config</artifactId>
				<version>${revision}</version>
			</dependency>
			<dependency>
				<groupId>de.spacepotato</groupId>
				<artifactId>sagittarius-log</artifactId>
				<version>${revision}</version>
			</dependency>
			<dependency>
				<groupId>de.spacepotato</groupId>
				<artifactId>sagittarius-nbt</artifactId>
				<version>${revision}</version>
			</dependency>
			<dependency>
				<groupId>de.spacepotato</groupId>
				<artifactId>sagittarius-network</artifactId>
				<version>${revision}</version>
			</dependency>
			<dependency>
				<groupId>de.spacepotato</groupId>
				<artifactId>sagittarius-viaversion</artifactId>
				<version>${revision}</version>
			</dependency>
			<dependency>
				<groupId>de.spacepotato</groupId>
				<artifactId>sagittarius-limbo</artifactId>
				<version>${revision}</version>
			</dependency>

			<!-- External dependencies -->
			<dependency>
				<groupId>org.projectlombok</groupId>
				<artifactId>lombok</artifactId>
				<version>${lombok.version}</version>
				<scope>provided</scope>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-all</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.logging.log4j</groupId>
				<artifactId>log4j-api</artifactId>
				<version>${log4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.logging.log4j</groupId>
				<artifactId>log4j-core</artifactId>
				<version>${log4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.logging.log4j</groupId>
				<artifactId>log4j-slf4j-impl</artifactId>
				<version>${log4j.version}</version>
			</dependency>
			<dependency>
				<groupId>com.google.code.gson</groupId>
				<artifactId>gson</artifactId>
				<version>${gson.version}</version>
			</dependency>
			<dependency>
				<groupId>com.moandjiezana.toml</groupId>
				<artifactId>toml4j</artifactId>
				<version>${toml4j.version}</version>
			</dependency>
			<dependency>
				<groupId>com.carrotsearch</groupId>
				<artifactId>hppc</artifactId>
				<version>${hppc.version}</version>
			</dependency>
			<dependency>
				<groupId>com.viaversion</groupId>
				<artifactId>viaversion</artifactId>
				<version>${viaversion.version}</version>
			</dependency>
			<dependency>
				<groupId>org.yaml</groupId>
				<artifactId>snakeyaml</artifactId>
				<version>${snakeyaml.version}</version>
			</dependency>
			<dependency>
				<groupId>com.google.guava</groupId>
				<artifactId>guava</artifactId>
				<version>${guava.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<!-- Shared across every module -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
		</dependency>
	</dependencies>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${maven.compiler.plugin.version}</version>
					<configuration>
						<source>1.8</source>
						<target>1.8</target>
					</configuration>
				</plugin>

				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-shade-plugin</artifactId>
					<version>${maven.shade.plugin.version}</version>
					<configuration>
						<transformers>
							<transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
								<mainClass>de.spacepotato.sagittarius.Main</mainClass>
							</transformer>
						</transformers>
						<artifactSet>
							<includes>
								<include>**</include>
							</includes>
						</artifactSet>
						<finalName>Sagittarius</finalName>
					</configuration>
					<executions>
						<execution>
							<phase>package</phase>
							<goals>
								<goal>shade</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>flatten-maven-plugin</artifactId>
					<version>${flatten.plugin.version}</version>
					<configuration>
						<updatePomFile>true</updatePomFile>
						<flattenMode>resolveCiFriendliesOnly</flattenMode>
					</configuration>
					<executions>
						<execution>
							<id>flatten</id>
							<phase>process-resources</phase>
							<goals>
								<goal>flatten</goal>
							</goals>
						</execution>
						<execution>
							<id>flatten.clean</id>
							<phase>clean</phase>
							<goals>
								<goal>clean</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>

		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>flatten-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>